steps:
- task: PowerShell@2
  displayName: Setup nativeless version
  inputs:
    targetType: 'filePath'
    filePath: '$(Build.SourcesDirectory)/Resources/Scripts/Setup nativeless version.ps1'
    arguments: '-packageDescription "$(NativelessPackageDescription)"'